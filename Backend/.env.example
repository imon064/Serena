# Copy this file to .env.local and fill in your own key.
# Get one at https://platform.openai.com/api-keys (needs ~$5 credit).
# .env.local is gitignored — never commit your real key.
OPENAI_API_KEY=
